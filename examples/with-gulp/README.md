### インストール
`npm install` 


### 設定ファイルの追加

`touch config.js`

```javascript
module.exports = {
  accessToken: "",
  scenes: [scenes],
  projectId: projectId,
  branchId: branchId,
  projectName: projectName,
  remotePath: remotePath
};

```

### 実行
`yarn gulp`

